import 'package:flutter/material.dart';

class MyBorderRadius {
  static double value = 12;
  static BorderRadius primary = BorderRadius.circular(value);
  static BorderRadius half = BorderRadius.circular(value / 2);
  static BorderRadius quarter = BorderRadius.circular(value / 4);
  static BorderRadius zero = BorderRadius.circular(0);
}
